const initialState = {
  user: null,
};

const userReducer = (state = initialState, action) => {
  switch (key) {
    case "LOGIN":
      return action.payload;

    default:
      break;
  }
};
export default userReducer;